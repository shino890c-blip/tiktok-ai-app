# Supabase接続設定
# このファイルをコピーして .env.local を作成し、実際の値を入力してください。
# .env.local はコミットしないこと（.gitignore で除外済み）。

# Supabaseプロジェクトの URL（ブラウザ・サーバー両方で使用）
NEXT_PUBLIC_SUPABASE_URL=

# Supabaseプロジェクトの anon / public キー（ブラウザから利用、RLSが適用される）
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabaseプロジェクトの service_role キー（サーバー専用。RLSをバイパスするため絶対に公開しないこと）
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI APIキー（台本・企画・note記事生成用。サーバー側のみで使用）
OPENAI_API_KEY=
